# user.h replacements
  - op: replace_in_file
    src: 'src/user.h'
    match: '@INCLUDES'
    replace: |
      #include "zmod4410_sensor.h"
      
  - op: replace_in_file
    src: 'src/user.h'
    match: '@MACROS'
    replace: |
        #define WIFI_SSID               ""
        #define WIFI_PWD                ""

        #define IO_USERNAME             ""

        #define EXAMPLE_MQTT_HOST       ""

        #define SENSOR_DATA_COUNT       ( 2 )
        #define ZMOD_IAQ_TOPIC          "/feeds/iaq"
        #define ZMOD_ECO2_TOPIC         "/feeds/eco2"
        #define USER_LED_TOPIC          "/feeds/led"

  - op: replace_in_file
    src: 'src/user.h'
    match: '@EXTERN_GLOBAL_VARIABLES'
    replace: |


  - op: replace_in_file
    src: 'src/user.h'
    match: '@FN_DECLARATION'
    replace: |

