# user.h replacements
  - op: replace_in_file
    src: 'src/user.h'
    match: '@INCLUDES'
    replace: |
      #include "hs300x_sensor.h"
      
  - op: replace_in_file
    src: 'src/user.h'
    match: '@MACROS'
    replace: |
        #define WIFI_SSID               ""
        #define WIFI_PWD                ""

        #define IO_USERNAME             ""

        #define EXAMPLE_MQTT_HOST       ""

        #define SENSOR_DATA_COUNT       ( 2 )
        #define TEMP_SENS_TOPIC         "/feeds/temperature"
        #define HUM_SENS_TOPIC          "/feeds/humidity"
        #define USER_LED_TOPIC          "/feeds/led"

  - op: replace_in_file
    src: 'src/user.h'
    match: '@EXTERN_GLOBAL_VARIABLES'
    replace: |


  - op: replace_in_file
    src: 'src/user.h'
    match: '@FN_DECLARATION'
    replace: |

    