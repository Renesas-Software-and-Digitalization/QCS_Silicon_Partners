# user.h replacements
  - op: replace_in_file
    src: 'src/user.h'
    match: '@INCLUDES'
    replace: |
      #include "fs1015_sensor.h"
      
  - op: replace_in_file
    src: 'src/user.h'
    match: '@MACROS'
    replace: |
        #define WIFI_SSID               ""
        #define WIFI_PWD                ""

        #define IO_USERNAME             ""
        #define IO_KEY             		  ""

        #define SENSOR_DATA_COUNT       ( 2 )
        #define FLOW_SENS_TOPIC         "/feeds/flow"
        #define FLOW_COUNT_TOPIC        "/feeds/flowcount"
        #define USER_LED_TOPIC          "/feeds/led"

  - op: replace_in_file
    src: 'src/user.h'
    match: '@EXTERN_GLOBAL_VARIABLES'
    replace: |


  - op: replace_in_file
    src: 'src/user.h'
    match: '@FN_DECLARATION'
    replace: |

